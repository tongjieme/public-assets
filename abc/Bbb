public static long currentTimestampMicros() {
    Instant now = Instant.now();
    
    long seconds = now.getEpochSecond();
    int nanos = now.getNano();
    
    // 正确计算：秒 * 1_000_000 + 纳秒 / 1000（保留微秒精度）
    return seconds * 1_000_000 + nanos / 1000;
}
