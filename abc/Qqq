function isTimePassed(timeString) {
    const now = moment();
    const comparisonTime = moment(timeString, 'HH:mm');
  
    if (now.isAfter(comparisonTime)) {
        return true;
    } 
    else {
        return false; 
    }
}
