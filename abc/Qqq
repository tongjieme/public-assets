const fg = require('fast-glob');

async function findYmlFiles(pattern) {
  try {
    const files = await fg(pattern);
    console.log(files);
  } catch (error) {
    console.error(error);
  }
}

// Call the function, replace './**/*.yml' with your custom directory path
findYmlFiles('./**/*.yml');
