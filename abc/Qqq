async function getYmlFilesFromDirs(dirList) {
    let allFiles = [];
    for(let dir of dirList) {
        const files = await getYmlFiles(dir);
        allFiles = allFiles.concat(files);
    }
    return allFiles;
}
